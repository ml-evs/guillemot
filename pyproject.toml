[project]
name = "guillemot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "logfire>=4.6.0",
    "pydantic-ai>=1.0.4,<2",
    "python-dotenv>=1.1.1",
    "optimade[http-client,cif,pymatgen]",
    "matplotlib>=3.10.6",
    "pandas>=2.3.2",
    "datalab-api",
    "pybaselines>=1.2.1",
]

[tool.uv]
dev-dependencies = [
    "pytest",
    "pre-commit",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

# Add a "guillemot" entrypoint that launches the app in main
[project.scripts]
guillemot = "guillemot.main:launch"

[tool.setuptools.packages.find]
where = ["src"]

# The system prompts are text files, and are no use to an installed guillemot unless
# they are installed with it.
[tool.setuptools.package-data]
"guillemot.prompts" = ["*.md", "fragments/*.md"]

# ignore F841 in ruff rules
[tool.ruff.lint]
select = ["E"]
ignore = ["E501", "E741"]
